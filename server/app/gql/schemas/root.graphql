schema {
  query: Query
  mutation: Mutation
}

type Query {
  self: User!
}

type Mutation {
  signIn(phone: String, password: String): AccessToken

  addMemberToFamily(input: AddMemberToFamilyInput!): Family
  createExpense(input: NewExpenseInput!): Expense
}
